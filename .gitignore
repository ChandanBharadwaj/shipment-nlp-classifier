# Environment — never commit real credentials
.env
*.env

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# HuggingFace / sentence-transformers model cache
.cache/
models/

# Logs
*.log
logs/

# IDE / editor
.vscode/
.idea/
.claude/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test artefacts
test_data.csv

# Docker volumes (if accidentally created locally)
pgdata/

# Large raw HS reference data (re-fetchable via scripts/fetch_public_data.py)
data/hs/usitc_hts_2024.json
data/hs/uk_chapters_raw/

# Diagnostics tool backups — rebalance_keyword_weights writes a timestamped
# CSV of the prior weights before each run so the operator can restore
# manually. Per-run state, not source.
ml-service/diagnostics/rebalance_backup_*.csv

# Windows / cygwin crash dumps
*.stackdump
